import { JSX } from "preact";
import { UploadWidgetConfigRequired } from "../../../../config/UploadWidgetConfig";
interface Props {
    addFiles: (files: File[]) => void;
    isImageUploader: boolean;
    options: UploadWidgetConfigRequired;
}
export declare const UploaderWelcomeScreen: ({ addFiles, options: { multi, locale }, isImageUploader }: Props) => JSX.Element;
export {};
